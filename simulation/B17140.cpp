#include <iostream>
#include <vector>
int main()
{
    int r = 0, c = 0, k = 0, A[100][100], B[101] = {
                                              0,
                                          };
    fill(A[0][0], A[100][100], 0);
    return 0;
}