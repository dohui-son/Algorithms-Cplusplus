#include <bits/stdc++.h>
using namespace std;

#define endl "\n";
int a, b, c;
map<pair<int, int>, int> m;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    cin >> a >> b >> c;
    int s = 0, e = 0;
    for (int i = 0; i < 3; i++)
    {
        cin >> s >> e;
        m.insert({make_pair(s, e), 1});
    }
    for (int i = 0; i < 3; i++)
    {
        for (int j = i + 1; j < 3; j++)
        {
            if (st[i])
        }
    }

    return 0;
}